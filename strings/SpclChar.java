package strings;

public class SpclChar {

	public static void main(String[] args) {
		//String s="$ja!va$&st%ar";
		
		/*
		 * String res=""; for(int i=0;i<s.length();i++) { if(s.charAt(i)>=65 &&
		 * s.charAt(i)<=122) { res=res+s.charAt(i); } } System.out.print(res);
		 */
		
		/*
		 * char ch[]=s.toCharArray(); for(int i=0;i<ch.length;i++) { if(s.charAt(i)>='A'
		 * && s.charAt(i)<='Z' || s.charAt(i)>='a' && s.charAt(i)<='z') {
		 * System.out.print(ch[i]); } }
		 */
		
		String[] str= {"Hello+","-^Java+","-Programmer"};
	}

}
